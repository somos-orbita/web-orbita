Failed to apply some patches via editor tool. Repro steps:
1) Attempted to update hero.component.ts via apply_patch -> success
2) Attempted to update hero.component.html via apply_patch -> success
3) Attempted to update hero.component.scss via apply_patch -> success
4) Multiple attempts to update home.component.html via insert_edit_into_file failed with repeated tool errors.

The home.component.html file still contains the original orbita-hero tag without the calendlyUrl input. You can update it manually to:

  <orbita-hero [calendlyUrl]="calendlyUrl"></orbita-hero>

If you want I can try applying that single-line change again.
